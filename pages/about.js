import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class AboutIndex extends Component {
  render() {
    return (
      <div>
        <h2>About spencer</h2>
      </div>
    );
  }
}

AboutIndex.propTypes = propTypes;
export default AboutIndex;
